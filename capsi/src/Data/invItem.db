CREATE TABLE product(
    id INT,
    name VARCHAR(100),
    price INT,
    PRIMARY KEY (id),
    quantity INT,
    description VARCHAR(1000),
    image VARCHAR(1000),
    category_id INT,
    sku VARCHAR(100)
);